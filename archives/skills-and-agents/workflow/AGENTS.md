# Workflow continuation rule

- 当用户消息仅为“继续”“继续做”“继续处理”“继续这个任务”等继续类指令时，不要只做口头确认。
- 先根据当前会话里最近一个未完成任务，立即执行下一步具体动作；优先使用可用工具，而不是只回复“好的，继续”。
- 只有在以下情况才允许只回复说明：
  - 缺少必要权限，必须等待用户点击 `Allow` 或 `Always allow`
  - 缺少继续执行所必需的关键信息
  - 外部依赖或网络异常导致当前动作无法继续
- 如果上一步刚被权限阻塞，继续类指令默认表示：再次尝试刚才的动作，或明确提醒用户需要批准哪一个权限。
